export const initialUrlAdmin = "/dashboard"; // this url will open after login for admin
export const initialUrlTenant = "/:tenantId/dashboard"; // this url will open after login for tenant
export const initialUrlUser = "/:tenantId/dashboard"; // this url will open after login for user

export const allRoles = [1, 2, 3, 4, 5];
export const adminRoles = [1, 2];
export const tenantRoles = [3, 4];
export const userRoles = [5];
